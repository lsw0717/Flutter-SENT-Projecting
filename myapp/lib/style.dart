import 'package:flutter/material.dart';

var text2 = TextStyle(color: Colors.black);

var theme = ThemeData(
    textTheme: TextTheme(bodyText2: text2),
    appBarTheme: AppBarTheme(
  color: Colors.white,
));
