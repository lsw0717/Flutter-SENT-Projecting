import 'package:flutter/material.dart';

class UserIfoRegi extends StatelessWidget {
  const UserIfoRegi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('유저 정보 등록 페이지 IS NOT USER'),),
    );
  }
}
